#include <stdio.h>

int main() {
    int num, i, fat = 1;
    printf("Digite um numero inteiro positivo: ");
    scanf("%d", &num);
    for (i = 2; i <= num; i++) {
        fat *= i;
    }
    printf("O fatorial de %d eh %d\n", num, fat);
    return 0;
}
